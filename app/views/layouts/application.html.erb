<!DOCTYPE html>
<html>
	<head>
	  <title>Omrails</title>
	  <%= stylesheet_link_tag "application", :media => "all" %>
	  <%= javascript_include_tag "application" %>
	  <%= csrf_meta_tags %>
	</head>
	<body>
				<%= render "layouts/header" %>
			<div class="container">
				<%# The "=" sign before flash.each places unknown curly braces inside the page, so I took off the "=" sign.%>
				<% flash.each do |name, msg| %>
				<%= content_tag(:div, msg, class: "alert alert-#{name}") %>
				<% end %>	
				<%= yield %>
				<%= render "layouts/footer" %>
			</div>
	</body>
</html>